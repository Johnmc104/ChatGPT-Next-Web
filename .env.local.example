# ===========================================
# Cloudflare AI Gateway + OpenRouter 配置示例
# ===========================================

# OpenRouter 通过 Cloudflare AI Gateway 的配置
# 注意：BASE_URL 不要包含 /v1/chat/completions，系统会自动添加

# 方式1：使用 OpenRouter 通过 Cloudflare AI Gateway
BASE_URL=https://gateway.ai.cloudflare.com/v1/fb058588200c44aa9fc8729aecd6f204/gw/openrouter/v1/chat/completions

# API Key（你的 OpenRouter API Key 或 Cloudflare 提供的 Key）
OPENAI_API_KEY=sk-or-v1-your-openrouter-api-key-here

# 自定义模型列表（使用 OpenRouter 的模型格式）
# 格式: model-name 或 +model-name (添加) 或 -model-name (移除)
CUSTOM_MODELS=

# 默认模型
DEFAULT_MODEL=openai/gpt-4o-mini

# 访问密码（可选）
# CODE=your-password

# 禁用 GPT-4（可选，设为 1 禁用）
# DISABLE_GPT4=

# 隐藏用户 API Key 输入框（可选，设为 1 隐藏）
# HIDE_USER_API_KEY=1
